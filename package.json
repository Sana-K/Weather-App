{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "Application to predict weather of  place.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "skc",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "requests": "^0.3.0"
  }
}
